# SMTP Configuration (for Gmail, use an App Password)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM_NAME=游泳場館管理系統

# Optional: use a deployed backend endpoint instead of Firebase Trigger Email.
# Leave empty for localhost /api/notify-lesson or Firestore mail fallback.
VITE_NOTIFY_LESSON_URL=
